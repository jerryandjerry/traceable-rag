"""Reserved module for the future Deep Research workflow.

The frontend control is intentionally hidden and this module is intentionally
not mounted by :mod:`visionagent.api.main`. Keeping an inert, importable module
preserves the extension point without exposing a broken HTTP endpoint.
"""
