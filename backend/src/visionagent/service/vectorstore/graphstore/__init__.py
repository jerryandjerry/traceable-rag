"""Knowledge-graph extraction, merge policy, and persistence coordination.

``service.py`` extracts entities and relations and applies the merge helpers in
``lightrag_utils.py``. Persisted formats and read operations belong to
``database/graph``.
"""
